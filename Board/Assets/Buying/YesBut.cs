using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class YesBut : MonoBehaviour
{
    // Start is called before the first frame update
    bool clicked;
    void Start()
    {
        clicked = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
